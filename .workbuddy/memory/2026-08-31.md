# 2026-08-31 工作记录

## 品牌化与文档（交巡智星）
- **项目更名**：正式定名「交巡智星——面向网站安全风险评估与敏感信息防泄露的智能巡检智能体」。
- **前端品牌植入**（`web/index.html`，单文件 SPA，实时读盘）：
  - 登录页、顶栏、页脚均改为新标题；Logo 以 base64 data URI 内联（`<img class="brand-logo-img">`），favicon 也用 logo。
  - 注入方式：`_inject_logo.py` 将 `logo.jpeg` 编码后替换占位符 `__LOGO_DATA_URI__`（已执行；该脚本已加入 .gitignore）。
  - **删除登录界面默认账号密码**：移除 HTML 中的「默认账号: admin / Admin@123」段落，以及 JS 登录失败提示里的同款明文（现已改为中性提示）。
- **README.md 重写**：据实际代码更新——域名级定时扫描已移除（统一到 AI 巡检规则）、新增 logutil 日志页、四级权限(含 rl)、多供应商 AI(DeepSeek/Kimi/GLM)、漏洞去重与工单聚合、项目结构同步。
- **新增 作品说明书.md**：8 节（摘要/概述/设计实现/测试分析/创新性/竞品/总结/参考文献），参考文献给免费 PDF 链接（OWASP Top10、CWE、Graph Databases、LLM4Vuln、Attention、DeepSeek-V3、NIST SP800-115）。
- **新增 reset_db.py**：独立 Neo4j 清空脚本，含 --confirm 安全阀与 --dry-run，未运行（按用户要求）。
- **更新 .gitignore**：忽略 bin/、logs/、__pycache__、config.test.yaml、各类临时验证脚本（_t_*、check_*、insp_*、verify_*、gen_fingerprints.py、replay.js、_inject_logo.py 等）与遗留 exe；保留 reset_db.py、源码、README、作品说明书、logo。

## 验证
- 前端占位符 0 残留、Admin@123 0 残留、JS 语法 OK、go build 通过；8080 已重启(PID 1842)并实时返回新标题。
- 注意：纯前端改动硬刷新即生效；Go 改动需重启（本次未改 Go，仅前端+文档）。

## 全面测试并产出 测试报告.md
- 按用户给定 5 维度（编译/接口/权限/去重/巡检流转）对当前代码实测：
  - `go build ./...` EXIT 0、`go vet ./...` EXIT 0、`go test ./...` EXIT 0（inspection/scanner/scheduler ok）、前端内联脚本 vm 语法 OK。
  - 接口：登录 200(rl=3)、/api/stats 200、/api/logs 200、/api/inspections/records 200。
  - 权限：无令牌 401、旧版管理员令牌(无rl) 200、rl=1→403、rl=2→200、rl=3→200。
  - 去重：deduped=866、total=1300、高185+中468+低213=866（与去重数一致）。
  - 巡检流转：手动触发 202，running→analyzing(t+64s)→success(t+121s，端到端~118s)；历史 failed 记录佐证 RecoverStaleRecords 启动回收生效。
- 测试环境：Win10 + Go1.21+Gin + Neo4j(127.0.0.1:7687) + DeepSeek 兼容接口(deepseek-v4-flash) + Node22/curl + 4 域名/29 扫描/1300 命中 + 多套 JWT。
- 临时 Node 测试脚本测试后已清理；报告存 `测试报告.md`。
