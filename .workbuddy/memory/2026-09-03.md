# 2026-09-03 工作日志

## 10. 优化巡检结果判定（AI 相关 vs 非 AI 相关分类）

- **需求**：由智能体调用的巡检，仅当"智能体不可用→兜底扫描且成功"才标 partial；其余非 AI 巡检扫描成功即 success；两类边界互不干扰。
- **改动文件**：
  - `internal/models/inspection.go`：新增 `InspectionTriggerAgent = "agent"` 常量；`TriggeredBy` 注释补 `agent`。
  - `internal/inspection/runner.go`：`execute()` 重写判定——`scanResults==nil`→`failed`；`isAgentDriven := triggered_by=="agent"`；新增 `judge(rec, rule, scanResults, agentDriven)` 取代 `applyFallback`：`!agentDriven`→`success`；`agentAvailable()`→`success`；否则→`partial`（并附"智能体不可用，已触发兜底扫描"说明）。新增 `agentAvailable()` 用 `aiMgr.Ready()`（任一已启用且已配置供应商即可用）。`fallbackFromScan` 摘要改为中性"本地评估：..."（去掉误导性"AI 不可用"）。
  - `internal/scheduler/scheduler.go`：新增 `TriggerByAgent(ruleID)`（标记 agent 来源）。
  - `internal/agent/tools_action.go`：`run_inspection_rule` 工具改调 `d.Sched.TriggerByAgent`（原先 `TriggerManual`→被标 manual，导致 agent 来源丢失）。
- **验证**：`go build`/`go vet`/`go test ./...` 全绿；重启服务（8030，PID 见后台任务）后手动触发巡检 `7da677b7-…`（靶场 8099），记录 `f4ce21fe-…` 最终 `status=success`（triggered_by=manual，summary 中性），确认不再误标 partial。agent→partial 分支仅在全供应商不可用时可达（本环境 GLM 可用故 agent 触发也会 success）。
- **影响**：彻底解决"所有巡检都标部分成功"的误判；status=success 重新可达（非死代码）。
