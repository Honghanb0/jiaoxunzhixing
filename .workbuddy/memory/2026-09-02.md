# 2026-09-02 工作日志

## 1. 重写《恒脑接入.md》（V2）

**任务**：阅读 `hengnao/恒脑接入.md` 并按当前项目架构做重大更新；硬性约束＝自研智能体不可删除、恒脑仅为优先外部能力、不得覆盖自研兜底职责；缺失信息须明确列出、禁止臆测。

**产出**：
- `hengnao/恒脑接入.md`（614 行，V2 重写）
- 旧版归档为 `hengnao/恒脑接入.v1归档.md`
- 官方 PDF 文本提取到 `hengnao/_extract/*.txt`（7 个文件，约 200K，pypdf 提取，可删）

**核心发现（重要，后续接入必读）**：

1. **V1 的接入点已不存在**：`internal/inspection/runner.go:158-173` 经此前改造已彻底不调用大模型，只做「扫描 + `applyFallback` 本地兜底」。V1 写的"在 Runner 内替换 DoRiskJudge"失效。
2. **恒脑有 TWO 套鉴权完全不同的接口**，V1 只写了 A 类：
   - **A 类 开放服务接口**（appKey + HmacSHA256 sign）：`/open/api/v2/agent/execute` 等，一次性输入→输出，**无工具调用能力，无法驱动本平台编排循环**。
   - **B 类 大模型问答 OpenAI 兼容**（`Authorization: Bearer <LLMKey>`）：`POST {host}/api/openai/v1/chat/completions`，支持 `tools`/`tool_calls`/流式，模型 `HengNao-v4`/`HengNao-r1`。**这是唯一能让恒脑进入自研智能体的通路**。
3. **架构定位纠正**：自研智能体是**编排层（不可替换）**，恒脑是**能力层（可插拔）**。V1 写的"恒脑主用、自研降级"在编排层是倒挂的。正确做法 M1＝恒脑作为 `ai.Provider` 置于 `fallback_order` 首位驱动自研智能体循环（只换大脑不换身体）。
4. **已核对代码的实现陷阱**：恒脑 B 类错误是 **HTTP 200 + body `code` 非 0**（401/-1/-28/-4）。而 `internal/ai/openai_compat.go:148` 仅当 `StatusCode>=400` 判错、`:371` 的 `retryable()` 只看 HTTP 状态码 → 恒脑限流会被当成"空响应"且**不重试不降级**。接入前必须改适配层。
5. **agentId 缺失的官方解法**：`POST /open/api/v2/agent/search`（`keyword`/`scope`/`withForbid`/`page`/`size`）支持按名称模糊搜索，`参数.txt` 记录的"平台 bug 无法获取 agentId"可绕过；但必须校验命中唯一性（`forbid=false`，多条则拒绝自动选择）。
6. **官方状态码全表 25 个**（V1 只有 9 个），另有 **`flag` 三态**：0 成功 / 1 可跳过 / 2 服务异常需停止调用等待重试 —— 比 code 更适合做熔断信号。
7. **官方文档自相矛盾**：异步链路接口列表写 `/open/api/v2/agent/task/submit`，curl 示例写 `/open/api/v2/agent/submit`（4 个接口均如此），未实测前不得实现异步。
8. **并发**：B 类按租户维度限流，默认 `openapiMaxConcurrent=10`，所有 LLMKey 共享，超限 `code=-1`。多资产规则在 `runner.go:66-83` 按域名并发展开，极易打满 → 需本侧信号量（建议 4）。

**缺失信息清单（已写入文档第 12 节，P0 阻塞）**：
- **M-01 LLMKey 未获取**（参数.txt 只有 appKey/appSecret/userAppKey）→ M1 主线无法联调
- **M-02 A/B 两类生产 base_url 未确认**（参数.txt 给 `https://www.das-ai.com`，PDF 示例全是 `http://127.0.0.1`）
- **M-03 异步接口路径官方自相矛盾**
- M-04 `/system/queryToken` 的 `tid` 来源未在 execute 响应中定义；M-05 A 类输出无结构契约（`data.results` 键取决于平台配置）；M-06 恒脑可用模型清单未确认；M-07 `参数.txt` 明文密钥未入 .gitignore；M-08~M-12 配额/网络/沙箱/超时/SSO。

**未做（超出本次范围，已在文档中标记）**：未修改 `.gitignore`（M-07）、未实现任何恒脑代码（接入尚未开始，全仓 grep `hengnao|恒脑` 零命中）。

## 2. 代码事实核对（供后续接入直接引用）

- 自研智能体：26 个工具；`agent.go:56-149` 双层循环；`agent.go:227-234` 工具调用为**提示词协议** `<tool_calls>`（非 OpenAI function calling）；`config.yaml:155` max_turns=24。
- `ai.Manager`：`manager.go:202-223`/`:318-359` 三策略降级；`provider.go:26-30,73` 供应商常量与 `BuiltinOrder`；`providers.go:25-36` 工厂 switch（新增恒脑需改）；`openai_compat.go:313` 端点＝`base_url + /chat/completions`（恒脑 B 类可直接套用）。
- `agent.Manager.Run(goal, provider)`：`internal/agent/manager.go:70-97`，`:74` 前置 `mgr.Ready()`；Deps 在 `:36-50` 装配（新增恒脑客户端在此注入）。
- `InspectionRecord`：`models/inspection.go:70-94`，`Provider`/`Model`（:78-79）**当前无人写入**可复用；多资产 `DomainIDs`（:19）+ `EffectiveDomainIDs()`（:35-51）。
- 配置 struct：`config.go:27-31` AgentConfig、`:63-80` AIConfig、`:83-91` ProviderConfig（**无 app_secret/agent_id/llm_key 字段** → 恒脑必须独立配置块，不能塞进 `ai.providers`）。

## 3. 恒脑真实联调 + 代码落地（Request B）

**五轮 Python 实测**（`hengnao/probe_hengnao{1..5}.py` → `probe_result{1..5}.txt`，智能体元数据 `hengnao_agents.json`）：

- **A 类 base_url 确认**＝`https://www.das-ai.com`（签名通过）；**B 类 `/api/openai/v1/chat/completions` 真实网关是 `https://gc.das-ai.com`**（非 www；www 返回 SPA HTML，gc 返回 `401 Invalid API key`＝端点存在）。B 类仍需独立 **LLMKey**（参数.txt 没有）→ M1 原方案阻塞。
- **异步路径实测确认**＝真实为 `/open/api/v2/agent/task/*`（文档示例 `/agent/submit` 报 `-99` 不存在）。
- **`tid` 在 `/agent/execute` 响应顶层存在** → M-04 消除。
- **`/open/api/chat` 可用作 M1 大脑入口**（无需 LLMKey）：`{"query":"..."}` → `data.answer`；但它是【漏洞知识库 RAG】，仅「产品/组件名+漏洞」类提问能答（"Redis 未授权访问漏洞"✅含CVE，"SQL注入"❌拒答），`code` 仍 0 须查 `Refused`。延迟 5.5~36s。
- **已验证两个可复用智能体**：
  - 安全分析简报小助手 `ab3ab0e5-...`：`inputs.input`=告警原文 → `results.report`=Markdown 简报（实测 47s，6200 字，质量好）。
  - 告警研判智能体 `5284373e-...`：`inputs.toolName/toolParams`；9 个工具仅 **ActiveAttack**（`results.toolOutput`=JSON串）与 **AlarmAnalyzeByRelated**（需 `name`，`results.answer`=Markdown）实测可用，其余因服务端模板/插件额度不可用。
  - 恶意IoC查询智能体偶发 `-504 llm connect failed`（恒脑侧不稳）。

**落地代码（全部编译通过 + Go 真实联调）**：
- `internal/hengnao/` 新包：`types.go`(信封/code 兼容 string|number/`flag`/拒答识别)、`client.go`(签名 + **HTTP200+body.code 判错** + flag=2 重试 + `insecure_skip_verify`)、`chat.go`(Chat/SystemInfo/Healthy)、`agent.go`(ExecuteAgent/SearchAgent/resolveAgent/JudgeAlarm/JudgeActiveAttack/BriefReport/IoCQuery/CaseIPQuery；**sid 必须真 UUID** 否则 -409)。
- `config.yaml` 加 `hengnao:` 段；`internal/config/config.go` 加 `normalizeHengNaoConfig`（凭证缺失自动降级未启用）+ `HengNao hengnao.Config` 字段 + import。
- `internal/ai/hengnao.go` 新 `hengnaoProvider`（实现 Provider 接口，拒答→error 走降级链），`manager.go` 加 `RegisterHengNao`/`HengNao`；`cmd/server/main.go` 调 `RegisterHengNao(cfg.HengNao)`。
- `internal/agent/tools_hengnao.go` 注册 3 个工具 `hengnao_judge`/`hengnao_report`/`hengnao_vuln_qa`（未启用不注册；错误统一 wrap 提示「改用本地研判」）。
- `internal/hengnao/live_test.go`（`HENGNAO_LIVE=1` 守卫）真实联调：TestLiveChat ✅ 通过，BriefReport/JudgeActiveAttack 初版因 sid 非 UUID 失败，已修正为 crypto/rand 标准 UUID。

**红线已守住**：恒脑仅能力层，自研智能体仍是唯一编排者+强制兜底；恒脑失败一律落本地研判。

**待补（M-01）**：取得 LLMKey 后可平滑把 M1 切到 B 类（同 ai.Provider 接口）。M-07：参数.txt 仍明文密钥，项目非 git 仓库无法 .gitignore 兜底，建议迁移环境变量。

## 4. 文档更新
- `hengnao/恒脑接入.md` 第 12 节 M-02/M-03/M-04/M-05 标「已实测解决」，新增 §14 实测记录（2026-09-02 五轮联调 + 落地状态）。

## 5. 巡检管线接入恒脑简报（M2 增强落地 + 编译修复）

**目标（用户最后一条"继续"）**：把"恒脑安全分析简报小助手"串进交巡智星真实巡检任务流——扫描出高危漏洞后自动调恒脑生成简报。

**新增/改动**：
- `internal/models/inspection.go`：`InspectionRecord` 加 `HengNaoReport`(neo4j:hengnao_report) / `HengNaoStatus`(ok|skipped|refused|error)。
- `internal/storage/inspection_repo.go`：`Create`/`Update`/`nodeToRecord` 同步读写两个字段（Cypher 属性名一致）。
- `internal/inspection/runner.go`：`execute()` 在 `applyFallback` 后、`recordRepo.Update` 前调 `r.enrichWithHengNao(ctx, rec, finalResult)`；新增 `enrichWithHengNao`（仅 HighCount>0 且 `aiMgr.HengNao()!=nil` 才调用；子超时 90s；失败只记 status、绝不阻断/改判定）+ `buildHengNaoAlarmText`（拼「资产+风险等级+发现统计+高危明细(≤10)」告警原文）。

**编译期修复（实际踩坑）**：原本 `tools_hengnao.go` 的 `hengnaoClientOf` 与工具构造函数直接依赖 `*hengnao.Client`，编译报 `ai.HengNaoClient 需类型断言` / `imported and not used`。**收敛方案**：inspection 与 agent 两包只依赖 `ai.HengNaoClient` 接口（`Manager.HengNao()` 返回该接口；`hengnaoProvider.HengNaoAgent()` 内部把具体 `*hengnao.Client` 交到上层），消除 inspection/agent 对 hengnao 的反向依赖。`go build ./...` 与 `go vet`（agent/ai/inspection/storage/models/hengnao）全部通过（exit 0）。

**红线校验**：简报只"附加"不"判定"——风险等级/发现数仍由本地 `applyFallback` 决定；恒脑超时/拒答/报错只置 `HengNaoStatus=error` 并打日志，不影响巡检状态与工单。

**文档回填**：`恒脑接入.md` §14.5 追加巡检接入+依赖收敛两条 ✅，新增 §14.6 巡检接入契约细则；§10 末尾加「实际落地与阶段分歧」说明（M1 未走 B 类、恒脑包独立注册、工具独立成可选工具）。

**验证局限**：当前 shell 无 `HENGNAO_APP_KEY`/`HENGNAO_APP_SECRET`，无法重跑 `live_test`；但本次改动纯类型接线（接口 vs 具体类型），无请求/响应逻辑变动，前序 `live_test` 3/3 仍有效。

## 6. README 同步恒脑接入（文档对外化）

将恒脑接入的进展/新功能/局限性写入对外 `README.md`（用户要求）：
- §二 核心特性新增「恒脑能力层接入」bullet；§6.4 多供应商 AI 提及恒脑并指向 §6.8。
- 新增 §6.8 恒脑能力层接入（4 小节：两套接口与协议/信封陷阱、三处接入形态、已验证能力、依赖方向编译约束）。
- §七 config.yaml 示例补 `hengnao:` 段（enabled/key 环境变量/timeout/retries/agents ID）+ 环境变量说明追加 `HENGNAO_APP_KEY`/`HENGNAO_APP_SECRET`。
- §八 项目结构补 `internal/hengnao/`。
- §九 常见问题补 3 条（恒脑未启用/无简报、拒答、status=error 不阻断）。
- §十 近期更新新增「2026-09 更新（恒脑能力层接入）」：进展 / 新功能 / 局限性（含 M-01、M-07）三段。

## 7. 启动项目 + 实测恒脑简报生成（端到端验证）

- **启动**：`go build -o ./bin/security-agent ./cmd/server`（30MB，exit 0）。Neo4j 本机已在运行（7687/7474 开放），无需重新拉起。注入 `HENGNAO_APP_KEY`/`HENGNAO_APP_SECRET`（从 `hengnao/参数.txt` 解析，长度 27/32）后台启动服务，日志确认：Neo4j 连接成功、加载 11509 指纹 + 14 漏洞规则、注册 3 个巡检定时任务、`Server starting on 0.0.0.0:8080`，恒脑 provider 随 `RegisterHengNao` 激活。
- **实测 恒脑安全分析简报小助手**：用项目内 `internal/hengnao.BriefReport`（与编译进服务的同包代码）把一份模拟巡检告警（oan.yngtxy.edu.cn：Redis 未授权访问 + kkFileView 任意文件读取）交给恒脑，返回 **10168 字节 / 5702 字符 Markdown 威胁情报报告，耗时 72 秒**。结构：事件解读 → 攻击过程还原 → 告警研判与误报判别 → 排查命令 → 安恒安全产品方案(WAF/防火墙/EDR/APT) → 运维阻断(iptables/powershell) → 软件修复。质量良好、命中真实漏洞场景。
- **落盘**：完整报告存 `hengnao_brief_report.txt`（验证用，可删）。临时探测程序 `hengnao_cmd_probe` 已清理（正式 `internal/hengnao/live_test.go` 已覆盖此能力，用法 `HENGNAO_LIVE=1 go test ./internal/hengnao/ -run TestLiveBriefReport -v`）。
- **结论**：恒脑能力层接入在运行态验证有效——服务可启动、恒脑简报生成链路（A 类 agent execute → 安全分析简报小助手）真实可用。M-01（B 类 LLMKey）仍未解决，但 M1 主线（A 类 /open/api/chat + agent 简报）已实测打通。

## 8. 彻底移除恒脑集成（2026-09-02 收尾，按最新要求）

- **指令**：用户最新要求——项目不再需要恒脑智能体，全仓删除所有恒脑相关代码/引用/配置/智能体工具及残留文件。
- **已删除**：`internal/hengnao/` 整包；`internal/ai/hengnao.go`；`internal/agent/tools_hengnao.go`；`manager.go` 的 `RegisterHengNao`/`HengNaoClient`/`HengNao()`；`config.go` 的 `HengNao` 字段/`normalizeHengNaoConfig`/`firstEnv`；`main.go` 的 `RegisterHengNao` 调用；`tools.go` 的 `registerHengNaoTools`；`runner.go` 的 `enrichWithHengNao`+`buildHengNaoAlarmText` 调用与函数；`models/inspection.go` 的 `HengNaoReport`/`HengNaoStatus`；`storage/inspection_repo.go` 的 3 处 Cypher 读写；`config.yaml` 的 `hengnao:` 配置段；`README.md` 的 §二/§6.4/§6.8/§七/§八/§九/§十 共 7 处恒脑章节；整个 `hengnao/` 残留目录（含 `恒脑接入.md`、探测脚本、`参数.txt` 明文密钥等）。
- **验证**：`go build ./...` 与 `go vet ./...` 均 exit 0；全仓 `grep -rin "hengnao|恒脑"` 在代码/config/README 中已零命中（仅 `.workbuddy/memory` 历史日志与 `request.txt` 原始赛题说明残留，均非集成代码）。
- **注意**：`runner.go` 的 `aiMgr *ai.Manager` 字段保留（仍为构造函数入参，仅不再被读取），`ai` 包导入仍被该字段类型使用，编译无误。自研智能体现为唯一编排层+强制兜底，恒脑能力层已完全剥离。

## 9. 端口改 8030 + 8099 靶场 + 全面测试 + 报告/README/.gitignore（2026-09-03 续作）

- **端口变更**：`config.yaml` 第 4 行 `port: 8080` → `port: 8030`（默认控制台）。所有功能测试基准地址 `http://127.0.0.1:8030`。
- **8099 靶场方案（关键坑位）**：平台 NoRoute 对裸路径 GET 回退 SPA `index.html`，故不能用平台实例在真路径暴露 `verify_site/` 夹具（裸 `/xss.html` 只返首页）。最终靶场 = **独立静态服务** `python -m http.server 8099 --bind 127.0.0.1 --directory verify_site`（PID 41660）。`config.8099.yaml` 仅作配置示例（port 8099、scheduler/agent 设 false 标注"被动目标"），不对外服务靶场。
- **编译/质量**：`go build -o ./bin/security-agent ./cmd/server` exit 0（~30MB）；`go vet ./...` exit 0；`go test ./...` exit 0（agent 4.309s / inspection 0.554s / scanner 1.984s / scheduler 0.542s）。
- **功能/权限**：`logs/func_test.py` 全 200（登录 rl=3 token303、/api/stats、/api/logs、/api/inspections/records、/api/domains count=7、/api/scan/rules total=14、/api/agent/tools 27 工具、8099 health）；权限 401(无令牌)/403(rl=1)/200(admin) 精确。
- **漏洞检测/误报率（核心）**：扫 `http://127.0.0.1:8099` → 15 漏洞（xss3/malicious_link2/tampering2/sqli2/sensitive_file6）+ 3 敏感信息；ground truth 比对 **15/15 规则覆盖漏洞全中、0 误报（FPR=0%）**，良性 `page1.html`/`page2.html` 未误报；仅 `webshell.html` 无对应规则未检出（召回 100% 规则口径 / 93.75% 含无规则项）。混淆矩阵 TP=15/FP=0/FN=1/TN=2。
- **智能体端到端**：`logs/agent_run.py` 只读"列出域名" → completed（2 步，glm-4.6，正确列 7 域名均 active），恒脑移除未损工具注册。
- **AI 巡检**：`logs/inspection_run.py` → `running→partial`（扫描 15 发现，AI 兜底 applyFallback 生效，设计内）。
- **去重口径观察**：实时看板 deduped=902，high+medium+low=204+483+214=901，差 1 条（deduped 含三档之外严重度），设计内。
- **交付物**：覆写 `测试报告.md`（§0 方案环境 / §1 编译单测 / §2 接口+权限 / §3 检测能力+误报率混淆矩阵 / §4 智能体 / §5 AI 巡检 / §6 去重 / §7 综合结论）；`README.md` 端口 8080→8030（3 处）+ 新增 §3.5 本地漏洞靶场(8099) 含 SPA 兜底坑位说明；`.gitignore` 追加 `token.txt`、`hengnao_brief_report.txt`（根级遗留，均被忽略）。
- **存活进程**：8030 主控制台 PID 22332、8099 静态靶场 PID 41660（撰写报告时 `/health` 仍 ok）。
