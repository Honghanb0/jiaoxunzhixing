# ============================================================
# 安全巡检自主智能体 (security-agent) —— .gitignore
# 适用于 Go + Gin + Neo4j 项目。提交前请确认：
#   - 含密钥/口令的配置文件已被忽略（仅提交 config.yaml.example 等样例）
#   - 编译产物、日志、本地数据不会进入版本库
# ============================================================

# ---------------- Go 编译产物 / 二进制 ----------------
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
*.cover
*.err
# 构建输出目录（go build -o ./bin/security-agent 会重新生成）
bin/

# ---------------- 含密钥的配置文件（禁止入库） ----------------
# 真实配置含 Neo4j 口令、AI 供应商 Key、JWT 密钥等，绝不能提交。
# 仅保留无密钥的样例（config.yaml.example）供他人参照。
config.yaml
config.*.yaml
# 恒脑等第三方密钥明文文件（如 hengnao/参数.txt）
hengnao/参数.txt

# ---------------- 日志 ----------------
*.log
logs/

# ---------------- 本地运行数据 / 临时产物 ----------------
# 登录令牌、扫描基线、原始请求包、运行错误输出等
login.json
baseline_*.json
request.txt
server_run.log
server_8030.log
range_8099.log
neo4j_console.log

# ---------------- 弱口令词表等本地数据 ----------------
passwd_pwn/

# ---------------- 项目记忆（可能含敏感信息） ----------------
.workbuddy/

# ---------------- 操作系统 / 编辑器垃圾 ----------------
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
*.swp
*~
.idea/
.vscode/

# ---------------- 运行期产物（2026-09-11 仓库清理追加） ----------------
*.err
logs/
security-ops-skill/output/*
!security-ops-skill/output/.gitkeep
